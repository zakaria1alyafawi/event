# Update Login Response Plan\n\n## Current Response\n```json\n{\n  \"message\": \"Login successful\",\n  \"token\": \"jwt...\",\n  \"UserType\": \"super_admin\",  // outdated, no field\n  \"FirstName\": \"Zakaria\",\n  \"LastName\": \"Alyafawi\"\n}\n```\n\n## Required Fields\n- Direct from [`UserModel`](Models/Users/Users.py): `email`, `phone`, `first_name`, `last_name`, `display_name`, `job_title`, `country`, `city`, `company_id`, `access_token`\n- From relationships: `role_id` (UserRoles.role_id), `type_name` (UserTypes.name) → likely **list** due to many-to-many (per event)\n\n## Data Flow\n```mermaid\ngraph TD\n    Req[POST /login {email,pass}] --> Login[LoginRoute.login]\n    Login --> Sess[get_session]\n    Sess --> Val[RetrieveUsers.validate_login] -->|user obj| Gen[AuthManager.gen_token(user.id)]\n    Gen --> AddS[AddSessions.add token 1h]\n    Val --> Full[RetrieveUsers.get_full_profile(user.id)] -->|user dict + roles list| Resp[success_response {fields + roles}]\n```\n\n## Changes\n1. **Models/Users/RetrieveUsers.py** [`RetrieveUsers.py`](Models/Users/RetrieveUsers.py):\n   - Add `get_full_user_profile(self, user_id)`:\n     ```python\nfrom sqlalchemy.orm import joinedload\n     user = self.session.query(UserModel).options(\n       joinedload(UserModel.user_roles).joinedload(UserRolesModel.role)\n     ).filter(UserModel.id == user_id).first()\n     if not user: return None\n     profile = {k: str(v) if hasattr(v, \'__str__\') else v for k,v in user.__dict__.items() if not k.startswith(\'_\') and k != \'password_hash\'}\n     profile[\'roles\'] = [{\n       \'role_id\': str(ur.role_id),\n       \'type_name\': ur.role.name,\n       \'event_id\': str(ur.event_id) if ur.event_id else None\n     } for ur in user.user_roles]\n     return profile\n     ```\n\n2. **API/routes/login.py** [`login.py`](API/routes/login.py):\n   - After `user = retriever.validate_login(...)` & token gen/addsession:\n     ```python\n     profile = retriever.get_full_user_profile(user.id)\n     return success_response({\n       \"message\": \"Login successful\",\n       \"token\": token,\n       **profile  // spreads email,phone,...roles\n     }, 200)\n     ```\n\n## Notes\n- UUID → str for JSON\n- Exclude photo_url? (not requested)\n- Assume primary/global roles if no event_id\n- Test: login → check response fields/roles\n- Edge: no roles → empty list